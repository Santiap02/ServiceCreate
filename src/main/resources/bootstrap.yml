spring:

  application:
    name: service-create
  cloud:
    config:
      uri: http://localhost:8888  #Localización Spring Cloud Config

spring:

  application:
    name: service-create
  cloud:
    config:
      uri: http://localhost:8888  #Localización Spring Cloud Config

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

eureka.